'use client';
import { View, Text } from 'react-native';

export default function Page() {
  return (
    <View style={{ padding: 24 }}>
      <Text>Web (Next.js) using react-native-web</Text>
    </View>
  );
}